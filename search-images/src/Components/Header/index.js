import './index.scss'

const Header = () => {
  const view = `
  <header class="header">
    <input placeholder="Buscador de imagenes: Escribe tu busqueda" />
  </header>
  `
  return view
}

export default Header
